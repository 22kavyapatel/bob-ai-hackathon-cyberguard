NOT DEPLOYED

The application runs locally. See docs/setup-guide.md for instructions.

To run locally:
  cd src/backend && uvicorn main:app --reload --port 8000
  cd src/frontend && npm install && npm run dev
  Open: http://localhost:5173
